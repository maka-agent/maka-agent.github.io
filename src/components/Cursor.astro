<div id="cursor" class="cursor" aria-hidden="true">
  <svg viewBox="0 0 26 26" width="26" height="26">
    <defs>
      <linearGradient id="cursor-body" x1="0" y1="0" x2="1" y2="1">
        <stop offset="0" stop-color="#6fa9ea"></stop>
        <stop offset="0.45" stop-color="#3d7fd9"></stop>
        <stop offset="1" stop-color="#1d5cb4"></stop>
      </linearGradient>
    </defs>
    <path
      d="M3.2 2.4 L23.4 11.9 L14.3 14.4 L10.2 23.0 Z"
      fill="url(#cursor-body)"
      stroke="#ffffff"
      stroke-width="1.4"
      stroke-linejoin="round"
    ></path>
    <ellipse cx="9.4" cy="8.2" rx="3.4" ry="1.9" fill="rgba(255,255,255,0.55)" transform="rotate(24 9.4 8.2)"></ellipse>
  </svg>
</div>

<style>
  .cursor {
    position: fixed;
    top: 10px;
    left: 10px;
    z-index: 60;
    pointer-events: none;
    opacity: 0;
    transition: opacity 0.4s ease;
    filter: drop-shadow(0 4px 10px rgba(29, 92, 180, 0.35));
    will-change: transform;
  }

  .cursor.is-active {
    opacity: 1;
  }

  @media (pointer: coarse) {
    .cursor {
      display: none;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .cursor {
      display: none;
    }
  }
</style>
