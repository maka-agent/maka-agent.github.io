<section class="hero" id="top" data-theme="sky" data-section="00 INTRO">
  <div class="hero-notes">
    <p class="hero-thesis">
      Direct real work.<br />Keep every decision visible.
    </p>
    <p class="hero-mono mono">
      Directed by you.<br />Executed on your machine.
    </p>
    <p class="hero-bio mono">
      Maka is an open-source workspace for directing and supervising agents.
      Conversation, Tool&nbsp;Calls, permissions, and generated artifacts stay
      attached to one durable, recoverable task.
    </p>
  </div>

  <h1 class="hero-statement" id="hero-title">
    <span class="line"><span>YOUR&nbsp;WORK.</span></span>
    <span class="line"><span>YOUR&nbsp;AGENT.</span></span>
  </h1>

  <a class="hero-cue mono" href="#work" data-nav="work">
    <span>01&nbsp;WORK</span>
    <b aria-hidden="true">↓</b>
  </a>
</section>

<style>
  .hero {
    position: relative;
    min-height: 100svh;
    display: flex;
    flex-direction: column;
    padding: calc(var(--shell-h) + 4vh) var(--edge) 96px;
  }

  .hero-notes {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 24px;
    align-items: start;
  }

  .hero-thesis {
    font-size: clamp(1.25rem, 1.9vw, 1.75rem);
    font-weight: 560;
    line-height: 1.3;
    letter-spacing: -0.015em;
  }

  .hero-mono {
    grid-column: 2;
    color: var(--ink-2);
    padding-left: 18px;
  }

  .hero-bio {
    grid-column: 4;
    max-width: 34ch;
    justify-self: end;
    color: var(--ink-2);
  }

  .hero-statement {
    margin-top: auto;
    font-size: clamp(3.1rem, 10vw, 10.5rem);
    font-weight: 790;
    line-height: 0.94;
    letter-spacing: -0.022em;
    text-transform: uppercase;
  }

  .hero-statement .line {
    display: block;
    overflow: hidden;
  }

  .hero-statement .line > span {
    display: inline-block;
    transform: translateY(0);
  }

  /* The statement rises only after the loader hands off. */
  :global(html.js) .hero-statement .line > span {
    transform: translateY(112%);
  }

  :global(html.js.is-loaded) .hero-statement .line > span {
    animation: rise 1s var(--ease-out) both;
  }

  :global(html.js.is-loaded) .hero-statement .line:nth-child(2) > span {
    animation-delay: 0.12s;
  }

  @keyframes rise {
    from {
      transform: translateY(112%);
    }
    to {
      transform: translateY(0);
    }
  }

  .hero-cue {
    position: absolute;
    right: var(--edge);
    bottom: 72px;
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 12px 0;
    text-decoration: none;
    color: var(--ink-2);
    letter-spacing: 0.1em;
  }

  .hero-cue b {
    font-weight: 500;
  }

  :global(html.js) .hero-cue b {
    animation: cue 2.2s ease-in-out infinite;
  }

  @keyframes cue {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(5px); }
  }

  .hero-cue:hover {
    color: var(--ink);
  }

  @media (max-width: 1023px) {
    .hero-notes {
      grid-template-columns: 1fr 1fr;
      row-gap: 32px;
    }

    .hero-mono {
      grid-column: 2;
      padding-left: 0;
      justify-self: end;
      text-align: right;
    }

    .hero-bio {
      grid-column: 1 / 3;
      justify-self: start;
      text-align: left;
    }
  }

  @media (max-width: 759px) {
    .hero {
      padding-bottom: 120px;
    }

    .hero-notes {
      grid-template-columns: 1fr;
    }

    .hero-mono,
    .hero-bio {
      grid-column: 1;
      justify-self: start;
      text-align: left;
      padding-left: 0;
    }

    .hero-statement {
      font-size: clamp(2.4rem, 12vw, 4.2rem);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    :global(html.js) .hero-statement .line > span {
      transform: none;
      animation: none;
    }

    :global(html.js) .hero-cue b {
      animation: none;
    }
  }
</style>
